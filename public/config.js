// XSLGPT Configuration Management

class ConfigManager {
    constructor() {
        // No API key management needed - handled by backend
    }

    // Placeholder methods for compatibility
    getApiKey() {
        return null; // API key is handled by backend
    }

    hasApiKey() {
        return true; // Always true since backend handles API key
    }
}

// Initialize global config manager
window.configManager = new ConfigManager(); 